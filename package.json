{
  "scripts": {
    "start": "snowpack dev",
    "build": "snowpack build",
    "test": "echo \"This template does not include a test runner by default.\" && exit 1"
  },
  "devDependencies": {
    "@snowpack/plugin-react-refresh": "^2.5.0",
    "snowpack": "^3.8.8",
    "snowpack-plugin-optimize": "^0.5.0"
  },
  "dependencies": {
    "babel-polyfill": "^6.26.0",
    "draft-js": "^0.11.7",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  }
}
